{
    "framework": "vite",
    "installCommand": "npm install && npm rebuild",
    "buildCommand": "npx vite build",
    "outputDirectory": "dist",
    "env": {
      "CI": "false"
    },
    "build": {
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
  